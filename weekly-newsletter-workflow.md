# 📋 AI×DX Weekly ニュースレター — 毎週月曜日の作業プロセス

VSCode上のClaude Codeで完結する、週刊ニュースレター発行の全工程マニュアル。

---

## 作業全体フロー図

```
Step 0  準備（ターミナル版）
  ↓
Step 1  ニュースレター生成を指示（ターミナル版）
  ↓
  ┌──── Claude Codeが自動実行 ────┐
  │  Exa検索 → コンテンツ構成      │
  │  → HTML生成 → 戻るボタン追加   │
  │  → Weeklyライブラリ更新        │
  └──────────────────────────────┘
  ↓
Step 2  生成物の確認・修正（拡張機能版）
  ↓
Step 3  ブラウザプレビュー（手動）
  ↓
Step 4  発行前構造監査（Claude Code）
  ↓
Step 5  Git commit & push（Sourcetree）
  ↓
Step 6  GitHub Pages で公開確認（手動）
  ↓
Step 7  作業記録の更新（拡張機能版）
```

---

## Step 0：準備（所要時間：1分）

**使用：ターミナル版**

VSCodeのターミナルを開き、プロジェクトフォルダにいることを確認する。

```bash
cd ~/weekly-newsletter
```

Claude Codeを起動する。

```bash
claude
```

MCP接続を確認する。

```
/mcp
```

`exa · ✓ connected` を確認。failedの場合はClaude Codeを再起動する。

---

## Step 1：ニュースレター生成を指示（所要時間：3〜5分）

**使用：ターミナル版**（Exa MCP が必要なため）

Claude Codeに以下のように指示する。

```
今週のニュースレターを作って
```

これだけでSKILL.mdに従って以下が自動実行される：

| 自動処理 | 内容 |
|---------|------|
| トピック収集 | Exa MCPでWeb検索を実行 |
| コンテンツ構成 | ニュースTOP3、ツール2件、用語1件、コラムを構成 |
| HTML生成 | テーブルレイアウト + レスポンシブ対応のHTML |
| 戻るボタン追加 | `</body>` 直前にナビゲーションボタン |
| Weeklyライブラリ更新 | ルートの `index.html` に新号エントリを追加 |
| ファイル出力 | `outputs/volX/index.html` に保存 |

### オプション指定がある場合の例

```
今週のニュースレターをVol.5で作って。特集テーマは「ノーコードツール」で。
```

```
今週のニュースレターを作って。Azure関連のニュースは除外して。
```

### 自動更新されるファイル一覧

| ファイル | 更新内容 |
|--------|--------|
| `outputs/volX/index.html` | 🆕 新規作成（ニュースレター本体） |
| `index.html`（ルート） | ✏️ Weeklyライブラリに新号を追加 |

---

## Step 2：生成物の確認・修正（所要時間：5〜10分）

**使用：拡張機能版**（エディタ連携で効率的）

ターミナル版で `/exit` してClaude Codeを終了する。

VSCodeのエクスプローラーで `outputs/volX/index.html` を開く。

### 確認ポイント（SKILL.md 品質チェックリスト準拠）

- ニュースは3件あるか
- 各ニュースに出典リンクがあるか（リンク切れがないか）
- ツール紹介は2件あるか
- 用語解説が1つあるか
- 編集者コラムがあるか
- 英語キーワードが各所に併記されているか
- 戻るボタンが `</body>` 直前にあるか
- レスポンシブ用class（content-cell, card-cell, section-heading, body-text, source-link）が付与されているか

### 修正が必要な場合

エディタで該当箇所を選択し、右側のClaude Code拡張機能パネルで修正を依頼する。

```
選択した箇所のニュース要約をもう少し具体的にして
```

```
このツール紹介に料金情報が抜けているので追加して
```

### ルートの index.html も確認

Weeklyライブラリページ（`index.html`）を開き、新号のエントリが正しく追加されているか確認する。

- Vol.番号が正しいか
- タイトル（ニュース見出し3本）が反映されているか
- 日付が正しいか
- リンク先 `outputs/volX/` が正しいか

---

## Step 3：ブラウザプレビュー（所要時間：2分）

**使用：手動操作**

VSCodeの `outputs/volX/index.html` を右クリック → 「Live Serverで開く」 
または、ターミナルで以下を実行：

```bash
open outputs/volX/index.html
```

### 確認ポイント

- PC表示（通常幅）でレイアウトが崩れていないか
- ブラウザのデベロッパーツール（F12）でスマホ表示を確認
- 戻るボタンが右下に表示され、`index.html` に遷移するか
- リンクが正しく開くか

---

## Step 4：発行前構造監査（所要時間：1〜2分）

**使用：ターミナル版**（Claude Code）

ブラウザプレビューで視覚確認が終わったら、Claude Code に構造監査を実行させる。

`docs/pre-publish-audit-prompt.md` の内容をそのまま Claude Code に貼り付けて実行する。

### 監査レポートの見方

| 判定 | 意味 | 対応 |
|-----|------|------|
| ✅ GO | 構造に問題なし | 次の Step へ進む |
| ⛔ NO-GO | 構造ミスあり | Claude Code が提示する修正コードを承認して修正後、次へ |

### 特に注意するポイント

- **📚 Weeklyライブラリへ** リンクがヘッダー直下にあったら即 NO-GO
- NEWS 01 → 02 → 03 の順序
- フッターが最下部にあるか

---

## Step 5：Git commit & push（所要時間：3分）

**使用：Sourcetree**（琉誠さんの通常ワークフロー：VSCode → Sourcetree → GitHub）

### 4-1. Sourcetreeを開く

weekly-newsletter リポジトリを開くと、変更されたファイルが「作業ツリーのファイル」に表示される。

### 4-2. 変更内容の差分を確認

以下のファイルが変更されているはず：

| ファイル | 状態 | 確認ポイント |
|--------|------|------------|
| `outputs/volX/index.html` | 🆕 新規追加 | ニュースレター本体 |
| `index.html`（ルート） | ✏️ 変更 | Weeklyライブラリに新号が追加されているか |

差分（diff）を確認して、意図しない変更がないかチェックする。

### 4-3. ステージに上げる

変更ファイルを選択して「ステージに上げる」（または全てインデックスに追加）。

### 4-4. コミット

コミットメッセージの例：

```
Vol.X ニュースレター追加（YYYY年MM月DD日号）
```

### 4-5. プッシュ

「プッシュ」ボタンで `origin/main` にプッシュ。

---

## Step 6：GitHub Pages で公開確認（所要時間：1分）

**使用：手動操作**

push後、1〜2分待ってからGitHub PagesのURLにアクセスして確認する。

```
https://（ユーザー名）.github.io/weekly-newsletter/
```

- Weeklyライブラリに新号が表示されているか
- 新号をクリックして正しく表示されるか
- 戻るボタンでWeeklyライブラリに戻れるか

---

## Step 7：作業記録の更新（所要時間：2分）

**使用：拡張機能版**

`work-log.md` を開き、今回の作業記録を追記する。

拡張機能版のClaude Codeに以下のように依頼：

```
work-log.mdに今回のVol.Xの作業記録を追加して。
日付は2026/XX/XX、特記事項は「（あれば記載）」
```

---

## 作業プロセスのまとめ表

| Step | 作業内容 | 使用ツール | 所要時間 |
|------|---------|-----------|---------|
| 0 | 準備・MCP確認 | ターミナル版 | 1分 |
| 1 | ニュースレター生成指示 | ターミナル版 | 3〜5分 |
| 2 | 生成物の確認・修正 | 拡張機能版 | 5〜10分 |
| 3 | ブラウザプレビュー | 手動 | 2分 |
| 4 | 発行前構造監査 | ターミナル版 | 1〜2分 |
| 5 | Git commit & push | Sourcetree | 3分 |
| 6 | GitHub Pages 公開確認 | 手動 | 1分 |
| 7 | 作業記録の更新 | 拡張機能版 | 2分 |
| | **合計** | | **約18〜26分** |

---

## トラブルシューティング

### Exa MCPが「failed」になる場合

```bash
# Claude Codeを終了してから
npm cache clean --force
# 再度 claude を起動
claude
```

### 号数が重複した場合

ターミナル版で以下を確認：

```
index.htmlのWeeklyライブラリで最新のVol.番号を教えて
```

### 生成されたHTMLの品質が低い場合

ターミナル版で追加指示：

```
Vol.Xのニュース1番目をもっと中小企業目線で書き直して
```
